# Postgres local (ver docker-compose.yml)
DATABASE_URL=postgres://pair:pair@localhost:5433/pair

# Identidad del Authorization Server propio (metadatos + endpoints).
OAUTH_ISSUER_URL=http://localhost:8787

# Base URL de apps/web, para el redirect a /oauth/consent.
PAIR_WEB_URL=http://localhost:3000
