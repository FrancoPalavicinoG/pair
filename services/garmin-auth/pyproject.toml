[project]
name = "garmin-auth"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi[standard]>=0.115.0", "garth==0.6.3", "pydantic-settings>=2.6.0"]

[dependency-groups]
dev = ["ruff>=0.8.0"]
